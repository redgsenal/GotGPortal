
extends layout

block layout-content
	h1 Game of the Generals
	div.player#black
		span BLACK
	table.board
		- for (var y = 8; y > 0; y--)			
			tr
				- for (var x = 1; x < 10; x++)
					if (y < 4)
						td.cell
							div.box.draggable.whitepiece(data-x=x, data-y = y, cellpadding='0px', cellspacing='0px')
							span.coor (#{x},#{y})
					else if (y > 5)
						td.cell
							div.box.draggable.blackpiece(data-x=x, data-y = y, cellpadding='0px', cellspacing='0px')
							span.coor (#{x},#{y})
					else
						td.cell
							div.box(data-x=x, data-y = y, cellpadding='0px', cellspacing='0px')
							span.coor (#{x},#{y})
	div.player#white
		span WHITE